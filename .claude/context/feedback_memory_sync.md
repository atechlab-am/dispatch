# Memory Sync Rule

Keep `.claude/context/MEMORY.md` (in the repo) in sync with Claude's internal memory files at:
`~/.claude/projects/-Users-amartins-Library-CloudStorage-OneDrive-Personal-GitHub-dispatch/memory/`

When either changes, update the other.

Update order when project state changes:
1. Internal memory files (e.g. `project_dispatch.md`)
2. `.claude/context/MEMORY.md` index

# Todo Sync Rule

Keep `.claude/context/todo.md` updated as tasks are added and completed.

- When starting work on an item, mark it `[~]`
- When complete, mark `[x]` and add a `CHANGELOG.md` entry
- New tasks discovered during implementation go into the appropriate phase section
